# Локальные настройки, необязательно. Остальное задано в docker-compose.yml.
# Скопируйте в .env, если нужно что-то поменять.

# Порт Postgres на localhost — если 5432 уже занят другим проектом
POSTGRES_PORT=5432
